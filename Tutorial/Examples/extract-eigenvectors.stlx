extractEigenVectors := procedure(a) {
    eigV := la_eigenVectors(a);
    rng  := [1 .. #eigV];
    return [ la_vector([eigV[row][col] : row in rng]): col in rng ];
};

a := la_matrix([[1, -1], [-1, 1]]);

print(a);
print( extractEigenVectors(a) );
print( la_eigenValues(a) );
