Fix this paragraph in "3.5.1 while Loops"

The function f is implemented via a while loop. This loop runs as long as n is different from
1. The syntax ...

There should be something after the "from" and there is the point #1, but no point #2!

======================

assert()

explain that the --noAssert (or -a) option disables assert statements entirely.
Thus they can be "switched off" for performance, when they perform complicated tests.
